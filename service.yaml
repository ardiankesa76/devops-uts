apiVersion: v1
kind: Service
metadata:
  name: devops-uts-service
spec:
  selector:
    app: devops-uts
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
